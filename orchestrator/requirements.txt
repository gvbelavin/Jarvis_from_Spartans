# requirements.txt — зависимости оркестратора (Задача А)

# Модуль 5 (Степан): индикатор на ESP32 общается по USB-serial.
pyserial>=3.5

# WeSpeaker (модуль Б) на старте импортирует torchaudio. Без пакета
# `python app.py` падает сырым ModuleNotFoundError уже после check_ready,
# если в списке был только torch. Ставить парой — pip подберёт совместимые
# версии. На плате (мало места): pip install --no-cache-dir -r requirements.txt
torch
torchaudio

# Всё остальное — стандартная библиотека Python:
#   asyncio, logging, dataclasses, argparse, pathlib, typing
#
# Остальные зависимости приходят с подмодулями:
#
#   orchestrator/audio_module/requirements.txt     (модуль Б)
#       numpy sounddevice soundfile openwakeword
#       wespeaker torch torchaudio piper-tts pywhispercpp
#
#   orchestrator/memory_module/requirements.txt    (модуль Ц)
#       пока пусто; на шаге 5 появятся sqlite-vec и onnxruntime,
#       на шаге 6 — requests
#
#   модуль Г (LLM): HTTP-клиент в llm_module.py, urllib из стандартной
#   библиотеки. Сервер Фёдора — отдельный процесс на порту 8080.
#
# Установка:
#   python -m pip install --no-cache-dir -r requirements.txt
#   python -m pip install -r audio_module/requirements.txt
#   python -m pip install -r memory_module/requirements.txt
